{
  "name": "abvdata",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/worker.js --format=esm --target=es2020",
    "publish": "wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^3.12.0",
    "esbuild": "^0.18.0"
  }
}
